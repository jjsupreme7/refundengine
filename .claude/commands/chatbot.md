Start the RAG chatbot with document server and feedback collection.

Starts:
- Flask document server (port 5001)
- Streamlit chatbot (port 8501)

Press Ctrl+C to stop both services.

```bash
./scripts/services/start_web_chatbot.sh
```
